export default class User {
    constructor(username, email, password, full_name) {
      this.username = username;
      this.email = email;
      this.password = password;
      this.full_name = full_name;
    }
  }
  